import React, {
  Component,
} from 'react';

export default class Shopping extends Component {
  // TODO: build the get, put, post for the shopping list
  // TODO: build the interface for the shopping list

  render() {
    return (
      <div>Shopping</div>
    );
  }
}
