import React, {
  Component,
} from 'react';

export default class Recipe extends Component {
  // TODO: build the get, post, and put for the recipe
  // TODO: build the interface for recipes

  render() {
    console.log("Recipe");
    return (
      <div>Recipe</div>
    );
  }
}
